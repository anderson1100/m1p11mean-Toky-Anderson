import { Component, OnInit } from '@angular/core';

@Component({
    selector: 'apointement-cmp',
    moduleId: module.id,
    templateUrl: 'apointement.component.html'
})

export class ApointementComponent implements OnInit{
    ngOnInit(){
    }
}
